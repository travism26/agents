apiVersion: v1
kind: Secret
metadata:
  name: agentic-email-tls
  namespace: agentic-email-writer
type: kubernetes.io/tls
data:
  # Demo Note: Replace these empty values with your actual base64-encoded TLS certificate and key
  # For production, generate proper TLS certificates and update these values
  tls.crt: ''
  tls.key: ''
